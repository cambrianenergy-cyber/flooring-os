export async function createEnvelopeFromEstimate(params: {
  estimateId: string;
  signerName: string;
  signerEmail: string;
  pdfUrl: string; // generated estimate PDF
}) {
  // TODO: implement DocuSign OAuth + envelopes:create
  // return envelopeId
  return "ENVELOPE_ID_PLACEHOLDER";
}
