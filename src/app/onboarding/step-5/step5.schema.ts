// Schema for onboarding step 5 (pricingDefaults)
export interface Step5Data {
  // Define your pricingDefaults fields here
  pricePerUnit: number;
  currency: string;
  // Add more fields as needed
}