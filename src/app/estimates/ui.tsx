// src/lib/actions.client.ts
export async function approveEstimate() {
  // static-first stub (no server)
  console.warn("approveEstimate stub (static build).");
  return { ok: true };
}

export async function rejectEstimate() {
  // static-first stub (no server)
  console.warn("rejectEstimate stub (static build).");
  return { ok: true };
}
